#include "salesman.h"
#include "matrix.h"
//
//int main() {
//    std::cout << "Hello, World!" << std::endl;
//    return 0;
//}

int main() {
//    // ????????????
//    int x, y;
//    cin >> x >> y;
//    int **adj_matrix = init_matrix(x, x);
//    for (int i = 0; i < x; i++) {
//        for (int j = 0; j < x; j++) {
//            adj_matrix[i][j] = 0;
//        }
//    }
//    fill_matrix(adj_matrix, y, x);
//    out_matrix(adj_matrix, x);
//    vector<sList *> min_way = build_tree(adj_matrix, x);
//    int locality = 0;
//    do {
//        cout << locality + 1 << " -> ";
//        locality = find_locality(min_way, locality);
//    } while (locality != 0);
//    cout << locality + 1;

    cout << for_matrix();
}